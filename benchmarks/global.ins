! This is an instruction file shared by the benchmarks using global PFTs.
! It simply imports the paths to the forcing data for the benchmarks and
! the standard global instruction file.

import "../data/ins/global_cru.ins"
import "paths.ins"
