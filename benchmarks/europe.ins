! This is an instruction file shared by the benchmarks using European species.
! It simply imports the paths to the forcing data for the benchmarks and
! the standard European instruction file.

import "../data/ins/europe_cru.ins"
import "paths.ins"
